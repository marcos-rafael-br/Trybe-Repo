//função comparativa que retorne true or false

//variavel que conte o numero de letras
//for para varrer de tras p frente e adicionar letras a uma variavel, para acessar letras preciso chamar variavel[i]

/* let nome = "arara";
let nomeRev = "";

function verificaPalindromo(nome) {
  for (i = nome.length - 1; i >= 0; i -= 1) {
    nomeRev = nomeRev += nome[i];
  }
  if (nome === nomeRev) {
    return true;
  } else {
    return false;
  }
}
console.log(verificaPalindromo(nome)); */

// Função recebe array de inteiros e retorna o indice do maior valor

/* function indiceDoMaior(array) {
  let indiceMaior = 0;
  for (let indice in array) {
    if (array[indiceMaior] < array[indice]) {
      // indice maior serve para setar a primeira posição de array e assim poder compará-la
      indiceMaior = indice;
    }
  }
  return indiceMaior;
}
console.log(indiceDoMaior([2, 3, 6, 7, 10, 1])); */
